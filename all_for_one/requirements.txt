# ─────────────────────────────────────────────────────────────
#  requirements.txt — Clustering Algorithm Comparison Pipeline
# ─────────────────────────────────────────────────────────────
#
#  Install everything:
#    pip install -r requirements.txt
#
#  GPU (optional, speeds up sentence-transformers):
#    pip install torch --index-url https://download.pytorch.org/whl/cu118

# Core ML / data
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# Embeddings
sentence-transformers>=2.7

# Clustering algorithms
minisom>=2.3.1          # SOM
bertopic>=0.16          # BERTopic (includes UMAP + HDBSCAN internally)

# Dimensionality reduction for visualisation
umap-learn>=0.5.5

# Visualisation
matplotlib>=3.8
plotly>=5.18            # needed by BERTopic visuals (HTML export)
